<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.potenter.instagram.user.dao.UserDAO">
	
	<insert id="insertUser" parameterType="Map">
		INSERT INTO `user`
		(
			`loginId`
			, `name`
			, `phoneNumber`
			, `password`
			, `createdAt`
			, `updatedAt`
		) VALUE (
			#{loginId}
			, #{name}
			, #{phoneNumber}
			, #{password}
			, now()
			, now()	
		)
	</insert>
	
	<select id="selectCountLoginId" parameterType="string" resultType="int">
		SELECT
			count(*)
		FROM
			`user`
		WHERE 
		 	`loginId` = #{loginId}	
	</select>
	
	<select id="selectUserByIdPassword" parameterType="map" resultType="com.potenter.instagram.user.model.User">
 		SELECT 
 			`id`
 			, `loginId`
 			, `password`
 			, `name`
 			, `createdAt`
 			, `updatedAt`
 		FROM
 			`user`
 		WHERE
 			`loginId` = #{loginId}
 			AND `password` = #{password}
 	</select>
	


</mapper>